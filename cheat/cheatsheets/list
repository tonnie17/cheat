list() -> new empty list

创建一个空列表。

list(iterable) -> new list initialized from iterable's items

用可迭代对象初始化一个列表。


# 创建一个空列表
arr = []

# 插入一个元素
arr.append(1)
[1]

# 合并一个列表
arr.extend([2,3])
[1, 2, 3]

# 尾部删除一个元素
arr.pop()
[1, 2]

# 往指定位置插入一个元素
arr.insert(1, 3)
[1, 3, 2]

# 排序一个列表
arr.sort()
[1, 2, 3]

# 从列表删除指定的元素
arr.remove(2)
[1, 3]

# 在列表查找指定元素的位置
arr.index(3)
1

# 反转一个列表
arr.reverse()
[3, 1]

# 查找列表中值为指定值的数量
arr.count(1)
1
